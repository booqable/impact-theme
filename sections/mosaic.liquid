{%- assign blocks = section.blocks -%}

{%- if blocks.size > 0 -%}

  {{ "mosaic.css" | asset_url | stylesheet_tag }}

  {%- assign button_label          = section.settings.button_label -%}
  {%- assign button_url            = section.settings.button_url -%}
  {%- assign title                 = section.settings.title -%}
  {%- assign color_palette         = section.settings.color_palette -%}
  {%- assign padding_top           = section.settings.padding_top -%}
  {%- assign padding_bottom        = section.settings.padding_bottom -%}
  {%- assign padding_top_mobile    = section.settings.padding_top_mobile -%}
  {%- assign padding_bottom_mobile = section.settings.padding_bottom_mobile -%}
  {%- assign image_placeholder     = settings.image_placeholder -%}

  {% comment %} CSS variables start {% endcomment %}
  {%- capture variables -%}
    {%- case padding_top -%}
      {%- when 'small' -%}
        --padding-top: 40px;
      {%- when 'medium' -%}
        --padding-top: 80px;
      {%- when 'large' -%}
        --padding-top: 112px;
    {%- endcase -%}

    {%- case padding_bottom -%}
      {%- when 'small' -%}
        --padding-bottom: 40px;
      {%- when 'medium' -%}
        --padding-bottom: 80px;
      {%- when 'large' -%}
        --padding-bottom: 112px;
    {%- endcase -%}

    {%- case padding_top_mobile -%}
      {%- when 'small' -%}
        --padding-top-mobile: 18px;
      {%- when 'medium' -%}
        --padding-top-mobile: 40px;
      {%- when 'large' -%}
        --padding-top-mobile: 60px;
    {%- endcase -%}

    {%- case padding_bottom_mobile -%}
      {%- when 'small' -%}
        --padding-bottom-mobile: 18px;
      {%- when 'medium' -%}
        --padding-bottom-mobile: 40px;
      {%- when 'large' -%}
        --padding-bottom-mobile: 60px;
    {%- endcase -%}
  {%- endcapture -%}
  {% comment %} CSS variables end {% endcomment %}

  <div
    class="mosaic__wrapper palette-{{ color_palette }}{% if padding_top != blank or padding_top_mobile != blank %} mosaic__wrapper--padding-top{% endif %}{% if padding_bottom != blank or padding_bottom_mobile != blank %} mosaic__wrapper--padding-bottom{%- endif -%}"
    style="{{- variables | escape -}}"
  >
    <div class="mosaic__container container">
      {%- if title != blank or button_label != blank and button_url != blank -%}
        <div class="mosaic__top">
          {%- if title != blank -%}
            <h2 class="mosaic__title h4">{{- title -}}</h2>
          {%- endif -%}

          {%- if button_label != blank and button_url != blank -%}
            <div class="mosaic__show-wrapper">
              <a href="{{ button_url }}" class="mosaic__show-button button button--outlined button--large">
                {{- button_label -}}
              </a>
            </div>
          {%- endif -%}
        </div>
      {%- endif -%}

      <div class="mosaic__list mosaic__list--{{- blocks.size -}}">
        {%- for block in blocks -%}
          {%- assign article_tag     = block.settings.article_tag -%}
          {%- assign heading         = block.settings.heading -%}
          {%- assign text            = block.settings.text -%}
          {%- assign article_btn     = block.settings.article_btn -%}
          {%- assign article_btn_url = block.settings.article_btn_url -%}
          {%- assign image           = block.settings.image -%}

          {%- if image != blank -%}
            {%- assign image = image -%}
          {%- elsif image_placeholder != blank -%}
            {%- assign image = image_placeholder -%}
          {%- else -%}
            {%- assign image = blank -%}
          {%- endif -%}

          <div class="mosaic__item" id="{{- block.id -}}">
            <div class="mosaic__image-wrapper{% if image == blank %} no-image{%- endif -%}">
              {%- if image != blank -%}
                {%- render 'image',
                  image: image,
                  loading: 'lazy',
                  class: 'mosaic__image',
                  size: 'm'
                -%}
              {%- endif -%}
            </div>

            {%- if article_tag != blank or heading != blank or text != blank or article_btn != blank and article_btn_url != blank -%}
              <div class="mosaic__text-area">
                {%- if article_tag != blank -%}
                  <strong class="mosaic__tag tagline">{{- article_tag -}}</strong>
                {%- endif -%}

                {%- if heading != blank -%}
                  <h3 class="mosaic__heading h5">{{- heading -}}</h3>
                {%- endif -%}

                {%- if text != blank -%}
                  <div class="mosaic__text bq-content rx-content">{{- text | truncate: 200, " ..."  -}}</div>
                {%- endif -%}

                {%- if article_btn != blank and article_btn_url != blank -%}
                  <div class="mosaic__buttons">
                    <a href="{{  article_btn_url }}" class="mosaic__button button button--primary button--large">
                      {{- article_btn -}}
                    </a>
                  </div>
                {%- endif -%}
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </div>

      {%- if button_label != blank and button_url != blank -%}
        <div class="mosaic__show-wrapper mosaic__bottom">
          <a href="{{ button_url }}" class="mosaic__show-button button button--outlined button--large">
            {{- button_label -}}
          </a>
        </div>
      {%- endif -%}
    </div>
  </div>
{%- endif -%}

{% schema %}
  {
    "name": "Mosaic",
    "description": "Add text with images in various layouts",
    "tag": "section",
    "class": "mosaic",
    "settings": [
      {
        "type": "header",
        "content": "General settings"
      },
      {
        "type": "select",
        "id": "color_palette",
        "label": "Color palette",
        "options": [
          {
            "value": "one",
            "label": "Color set 1"
          },
          {
            "value": "two",
            "label": "Color set 2"
          },
          {
            "value": "three",
            "label": "Color set 3"
          },
          {
            "value": "four",
            "label": "Color set 4"
          },
          {
            "value": "five",
            "label": "Color set 5"
          }
        ],
        "default": "four"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "What's in the news"
      },
      {
        "type": "header",
        "content": "Buttons"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Button label",
        "default": "See all Articles",
        "info": "Leave blank to hide the button"
      },
      {
        "type": "url",
        "id": "button_url",
        "label": "Button link"
      },
      {
        "type": "header",
        "content": "Desktop settings"
      },
      {
        "type": "select",
        "id": "padding_top",
        "label": "Padding top",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "select",
        "id": "padding_bottom",
        "label": "Padding bottom",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "header",
        "content": "Mobile settings"
      },
      {
        "type": "select",
        "id": "padding_top_mobile",
        "label": "Padding top",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "small"
      },
      {
        "type": "select",
        "id": "padding_bottom_mobile",
        "label": "Padding bottom",
        "options": [
          {
            "value": "none",
            "label": "None"
          },
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "small"
      }
    ],
    "max_blocks": 6,
    "blocks": [
      {
        "type": "article",
        "name": "Article",
        "settings": [
          {
            "type": "header",
            "content": "General settings"
          },
          {
            "type": "text",
            "id": "article_tag",
            "label": "Tagline",
            "default": "Tagline",
            "info": "Leave blank to hide tagline"
          },
          {
            "type": "text",
            "id": "heading",
            "label": "Title",
            "default": "Medium length section heading goes here"
          },
          {
            "type": "contentEditor",
            "id": "text",
            "label": "Description",
            "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros elementum tristique. Duis cursus, mi quis viverra ornare, eros dolor interdum nulla, ut commodo diam libero vitae erat."
          },
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image"
          },
          {
            "type": "header",
            "content": "Buttons"
          },
          {
            "type": "text",
            "id": "article_btn",
            "label": "Button label",
            "info": "Leave blank to hide the button",
            "default": "Read more"
          },
          {
            "type": "url",
            "id": "article_btn_url",
            "label": "Button link"
          }
        ]
      }
    ]
  }
{% endschema %}
